variable "upload_directory" {
    description = "Path to the files which are uploaded"
    type = string 
}

variable "bucket_name" {
    description = "To which bucket the files are uploaded to"
    type = string 
}